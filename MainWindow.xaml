<Window x:Class="CrystalLens.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CrystalLens"
        mc:Ignorable="d"
        Title="Crystal Lens" Height="450" Width="800">
    <Grid>
        <DockPanel>
            <Menu DockPanel.Dock="Top">
                <MenuItem Header="_File">
                    <MenuItem Header="_Open..." Click="OpenFile"/>
                    <Separator/>
                    <MenuItem Header="_Save" IsEnabled="False"/>
                    <MenuItem Header="Save _As..." IsEnabled="False"/>
                    <Separator/>
                    <MenuItem Header="_Close"/>
                    <Separator/>
                    <MenuItem Header="E_xit"/>
                </MenuItem>
            </Menu>
            <TabControl>
                <TabItem Header="Untitled">
                    <StackPanel Background="#FFE5E5E5">
                        <DataGrid Name="encounterTableDisplay" IsReadOnly="True" AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                                <DataGridTextColumn Header="Level" Binding="{Binding LevelDisplay}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </TabItem>
            </TabControl>
        </DockPanel>
    </Grid>
</Window>

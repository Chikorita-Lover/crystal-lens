<UserControl x:Class="CrystalLens.Views.EncounterSetMapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CrystalLens.Views"
             mc:Ignorable="d">
    <DockPanel>
        <ComboBox DockPanel.Dock="Top" ItemsSource="{Binding MapNames}" SelectedItem="{Binding SelectedMap, Mode=TwoWay}"/>
        <local:EncounterSetView DataContext="{Binding SelectedEncounters, UpdateSourceTrigger=PropertyChanged}"/>
    </DockPanel>
</UserControl>

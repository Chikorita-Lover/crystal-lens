<UserControl x:Class="CrystalLens.Views.PokemonSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CrystalLens.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DataGrid ItemsSource="{Binding Entries}" AutoGenerateColumns="False" IsReadOnly="True"
              CanUserResizeColumns="False" CanUserResizeRows="False"
              HorizontalGridLinesBrush="#ACACAC" VerticalGridLinesBrush="#ACACAC">
        <DataGrid.Columns>
            <DataGridTemplateColumn Header="Image" CanUserSort="False">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <local:Sprite DataContext="{Binding FrontSprite}"/>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            <DataGridTextColumn Header="Species" Binding="{Binding Name}"/>
            <DataGridTextColumn Header="BST" Binding="{Binding BaseStatTotal}"/>
            <DataGridTextColumn Header="Catch rate" Binding="{Binding CatchRate}"/>
            <DataGridTextColumn Header="Base EXP yield" Binding="{Binding BaseExp}"/>
            <DataGridTextColumn Header="Egg cycles" Binding="{Binding EggCycles}"/>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>

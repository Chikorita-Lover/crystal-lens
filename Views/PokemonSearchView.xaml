<UserControl x:Class="CrystalLens.Views.PokemonSearchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CrystalLens.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DataGrid ItemsSource="{Binding Entries}" IsReadOnly="True"
              CanUserResizeColumns="False" CanUserResizeRows="False"
              HorizontalGridLinesBrush="#ACACAC" VerticalGridLinesBrush="#ACACAC"
              AutoGeneratingColumn="DataGrid_AutoGeneratingColumn">
        <DataGrid.Columns>
            <DataGridTemplateColumn Header="Image" CanUserSort="False" DisplayIndex="0">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <local:Sprite DataContext="{Binding FrontSprite}"/>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
            <DataGridTextColumn Header="Species" Binding="{Binding Name}" DisplayIndex="1"/>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
